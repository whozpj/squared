services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: squared
      POSTGRES_PASSWORD: squared
      POSTGRES_DB: squared
    ports:
      - "5432:5432"
    volumes:
      - squared_pg:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U squared"]
      interval: 5s
      timeout: 3s
      retries: 10

volumes:
  squared_pg:

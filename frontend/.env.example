# Copy to .env.local for local overrides (optional).
# Leave empty in dev — Vite proxies API calls to the backend.
# In production set this to the deployed backend origin, e.g.
# VITE_API_BASE=https://squared-api.onrender.com
VITE_API_BASE=
